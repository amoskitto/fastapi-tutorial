version: "3"
services:
    api:
        build: .
        ports:
            - "8000:8000"
        depends_on:
            - db
        env_file:
            - .env
        environment:
            DATABASE_URL: postgresql://postgres:amoskitto88@db/fastapi
            DATABASE_HOSTNAME: db
            DATABASE_PORT: 5432
            DATABASE_USERNAME: postgres
            DATABASE_PASSWORD: amoskitto88
            DATABASE_NAME: fastapi
        volumes:
            - ./:/usr/src/app:ro
        command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    db:
        image: postgres:15
        environment:
            POSTGRES_PASSWORD: chikodiri2667amos
            POSTGRES_DB: fastapi
            POSTGRES_USER: postgres
        volumes:
            - postgres-db:/var/lib/postgresql/data

volumes:
    postgres-db:
